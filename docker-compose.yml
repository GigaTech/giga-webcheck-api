services:
  giga-webcheck-api:
    build: giga-webcheck-api
    ports:
      - "8086:3000"
    volumes:
      # - app:/app
      - app2:/home/node/app
volumes:
  # app:
  app2:
#     driver: local
